server {
    listen 80;
    server_name pioneeracademy.academy www.pioneeracademy.academy;

    # Permettre la validation Let's Encrypt (Certbot)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirection vers HTTPS (Une fois SSL activé)
    # location / {
    #     return 301 https://$host$request_uri;
    # }
    
    # En attendant SSL, on sert en HTTP
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Configuration HTTPS (A décommenter une fois les certificats générés)
# server {
#     listen 443 ssl;
#     server_name pioneeracademy.academy www.pioneeracademy.academy;
#
#     ssl_certificate /etc/letsencrypt/live/pioneeracademy.academy/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pioneeracademy.academy/privkey.pem;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location /api/ {
#         proxy_pass http://backend:3001;
#         # ... headers ...
#     }
# }
